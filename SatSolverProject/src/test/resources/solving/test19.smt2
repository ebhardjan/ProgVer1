(set-option :produce-models true)
(set-logic QF_UF)
(declare-fun p1() Bool)
(declare-fun p2() Bool)
(declare-fun p5() Bool)
(declare-fun p3() Bool)
(declare-fun p0() Bool)
(assert
(and
  (or (not p2) p1)
  (or p2 (not p1))
  (or p5 (not p0) p3)
  (or (not p5) p2)
  (or (not p1) (not p2))
  (or p0 p2)
  (or (not p3) p1)
))
(check-sat)
(get-model)

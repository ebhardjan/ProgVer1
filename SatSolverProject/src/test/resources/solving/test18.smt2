(set-option :produce-models true)
(set-logic QF_UF)
(declare-fun p2() Bool)
(declare-fun p1() Bool)
(declare-fun p4() Bool)
(declare-fun p0() Bool)
(declare-fun p5() Bool)
(assert
(and
  (or (not p2) (not p1))
  (or p2 p0)
  (or p2 (not p0))
  (or (not p2) p0)
  (or (not p0) p4)
  (or p5 p0)
  (or (not p0) p1 (not p4))
))
(check-sat)
(get-model)
